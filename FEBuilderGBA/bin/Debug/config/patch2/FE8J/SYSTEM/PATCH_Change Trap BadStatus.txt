
NAME=トラップで受けるステータス異常(バッドステータス)の種類
NAME.en=Trap bad status Type

//アドレスパッチとして実装します
TYPE=ADDR
TAG=#ENGINE

ADDRESS=0x3659E
DEFAULT=1
ADDRESS_TYPE=BADSTATUS

AUTHOR=7743

INFO=トラップが直撃した時に設定されるバットステータスの値を設定します。ディフォルトは1(毒)です。
INFO.en=Sets the value of the bat status that is set when trap is directly hit. The default is 1 (poison).
INFO.zh=Sets the value of the bat status that is set when trap is directly hit. The default is 1 (poison).

NAME.zh=Trap bad status Type
