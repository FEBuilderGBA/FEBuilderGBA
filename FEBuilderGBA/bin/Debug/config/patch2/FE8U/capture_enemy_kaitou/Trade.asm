@thumb
@org	$08025222
	;;自軍は交換可。友軍は交換不可。敵軍は同行者が居る場合は可
	;バサークされた味方の交換に対応
	ldr	r2, [r5]
	ldrb	r0, [r4, #11]	;相手の部隊ID
	lsl	r1, r0, #25
	bmi	$080252c4	;相手が緑軍なら不可
	ldrb	r1, [r4, #27]
	ldrb	r3, [r2, #11]
	cmp	r1, r3
	ldr	r3, [r4, #4]
	beq	$08025258	;救出者ならバサーク判定越え
	lsl	r0, r0, #24
	bpl	ok	;味方
;敵
	cmp	r1, #0
	beq	$080252c4	;相手が同行者無しなら不可
ok
	ldr	r0, [r2, #4]
	ldrb	r0, [r0, #4]
	cmp	r0, #81
	beq	$080252c4	;;自分が亡霊なら交換不可能
	
	
